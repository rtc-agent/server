Sub agent created successfully (async mode).
- Session ID: {{.SessionID}}
- Title: {{.Title}}

The sub agent is working asynchronously in the background. It will notify you when complete.

You can manage this sub agent using the following tools:
- **sendMessageToSubAgent**: Send additional instructions or context to the sub agent mid-execution.
- **listSubAgent**: List all running sub agents and their statuses.
- **stopSubAgent**: Stop the sub agent if the task is no longer needed.
