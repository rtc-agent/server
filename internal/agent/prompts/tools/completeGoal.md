Mark the current active goal as completed.

Call this when you have VERIFIED that the goal condition is fully satisfied (e.g., run tests, check outputs, lint code). Do NOT skip verification steps before calling this tool.

IMPORTANT: This is a mandatory step. If the goal condition is met, you MUST call this tool. Do not skip it.