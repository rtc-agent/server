# Find

Search for files by name or path pattern in the virtual filesystem.

## Usage Notes

- Finds files matching a name pattern (glob supported) within the workspace
- Returns matching file paths
- Use this to locate files before reading them
- For searching file contents, use the `grep` tool instead
- For listing directory contents, use the `ls` tool instead
