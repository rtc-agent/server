# Ls

List directory contents from the virtual filesystem.

## Usage Notes

- Returns files and directories at the specified path
- Use this to understand the structure of a directory before reading or writing files
- Prefer this over script-based directory listing approaches
- If the path does not exist, an error will be returned
